name: CI/CD

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  # ---- Backend: lint + unit test + build ----
  backend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.25'

      - name: Download dependencies
        run: go mod download

      - name: golangci-lint
        uses: golangci/golangci-lint-action@v6
        with:
          version: v1.60
          args: --timeout 5m

      - name: Unit tests
        run: go test -v -race -coverprofile=coverage.out ./...

      - name: Build
        run: CGO_ENABLED=0 go build -o /tmp/watchdog ./cmd/server

      - name: Upload coverage
        if: github.ref == 'refs/heads/main'
        uses: actions/upload-artifact@v4
        with:
          name: coverage
          path: coverage.out

  # ---- Backend: integration tests (need Docker) ----
  backend-integration:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.25'

      - name: Download dependencies
        run: go mod download

      - name: Install testcontainers for integration tests
        run: go get github.com/testcontainers/testcontainers-go github.com/testcontainers/testcontainers-go/modules/postgres

      - name: Integration tests
        run: go test -v -race -tags integration ./internal/api/...

  # ---- Frontend: lint + test + build ----
  frontend:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: web
    steps:
      - uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: web/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npx eslint src/ --ext .jsx,.js || true

      - name: Test
        run: npm run test

      - name: Build
        run: npm run build
        env:
          VITE_API_URL: https://watchdog-backend-production-dcde.up.railway.app

      - name: Upload build artifacts
        if: github.ref == 'refs/heads/main'
        uses: actions/upload-artifact@v4
        with:
          name: frontend-dist
          path: web/dist

  # ---- Frontend: Playwright E2E tests ----
  e2e:
    runs-on: ubuntu-latest
    needs: [backend, frontend]
    steps:
      - uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: web/package-lock.json

      - name: Install dependencies
        run: cd web && npm ci

      - name: Install Playwright
        run: cd web && npx playwright install --with-deps chromium

      - name: Build frontend
        run: cd web && npm run build
        env:
          VITE_API_URL: https://watchdog-backend-production-dcde.up.railway.app

      - name: Run E2E tests
        run: cd web && npx playwright test
        env:
          E2E_BASE_URL: https://watch-dog-omega.vercel.app

  # ---- Deploy (main branch only) ----
  deploy:
    needs: [backend, backend-integration, frontend]
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Deploy backend to Railway
        uses: bervProject/railway-deploy@v3
        with:
          service: watchdog-backend
          token: ${{ secrets.RAILWAY_TOKEN }}
          definition_file: railway.json

      - name: Deploy frontend to Vercel
        run: |
          cd web
          npm i -g vercel
          vercel --prod --yes --token ${{ secrets.VERCEL_TOKEN }}
        env:
          VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}
          VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}